# docs/archive

Historical planning, blueprint, and migration documents live here.

- These files describe past states, migration strategy, and refactor checkpoints.
- Do not treat them as the current implementation baseline.
- Use `app/docs/`, `web/docs/`, and `docs/architecture/` for the live architecture.
