# SOIT 重构现状说明

> 2026-03-08 的阶段性任务清单已归档至 `docs/archive/`。
> 当前活跃实现已统一采用 `knowledge` 语义，公开接口、前端路由、内部运行时命名均不再使用旧知识库命名。

当前状态：

- 知识库公开能力统一为 `knowledge`
- `workflow`、`run`、`trace`、`permission` 已对齐 `knowledge` 资源语义
- 前端页面、服务、E2E 命名已切换到 `knowledge`
- 数据库表与主外键已迁移为 `knowledge` / `knowledge_id`

后续维护原则：

- 不再新增任何旧知识库命名兼容层
- 历史旧术语仅允许保留在 `docs/archive/` 与 Alembic 历史迁移中
- 所有新增实现、文档、测试统一使用 `knowledge`
