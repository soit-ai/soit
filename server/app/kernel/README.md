# kernel/

SOIT stable core (compatibility-first). Kernel MUST NOT depend on `modules/`.

Key responsibilities:
- scope & identity primitives
- execution semantics
- trace (run/step/cost/artifact)
- ports and security enforcement
- spec schemas and registry mechanisms
