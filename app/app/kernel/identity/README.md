# kernel/identity/

Identity primitives:
- RequestContext resolution contracts
- token/session primitives
- role/permission core definitions

Product-level identity management (invites, member mgmt) belongs to domains.
