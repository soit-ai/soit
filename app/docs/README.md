# docs/ (in-app)

Engineering and architecture assets used by the backend repo.
Prefer keeping kernel-related docs here so versioning matches code.

- `architecture/PROJECT_STRUCTURE.md`: project structure overview
- `architecture/workflow_v1.md`: canonical workflow.v1 spec and publish pipeline
- `agent/README.md`: agent demo usage
- `security/README.md`: security API (egress + usage limits)
- `secrets/README.md`: secrets management API (workspace scope)
- Event bus backend can be configured via `EVENT_BUS_BACKEND` (memory/redis).
