# routes/knowledge/

Knowledge-centered frontend routes.

- The workspace landing page reads `GET /api/v1/knowledge/workbench` for metrics, tabs, and sidebar stats.
- The right-side table reads `GET /api/v1/knowledge/workbench/items` for filtered, paginated rows.
